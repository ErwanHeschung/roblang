[package]
name = "rob_parser"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Recursive-descent parser for the Rob language."

[dependencies]
rob_ast = { path = "../rob_ast" }
rob_lexer = { path = "../rob_lexer" }

[dev-dependencies]
insta = { workspace = true }
